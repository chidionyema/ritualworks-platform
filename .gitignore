# .NET
bin/
obj/

# Stryker mutation testing output
StrykerOutput/
*.user
*.suo
.vs/
.vscode/launch.json
.idea/
artifacts/
TestResults/
coverage*.xml
*.coverage
*.coveragexml

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
*.nuget.props
*.nuget.targets

# Aspire
.aspire/
*.AppHost/obj/
*.AppHost/bin/
deploy/aspire/vault-creds/

# OS
.DS_Store
Thumbs.db
*.swp
*~

# Logs
*.log
logs/

# Secrets — never commit
*.pem
*.key
*.pfx
.env
.env.*
!.env.example
secrets/
deploy/vault/dev-tokens/

# Image digest lock changes via auto-PR
# infra/image-digests.lock  ← committed; Renovate updates

# Pact
# Generated pact JSON is committed-by-default at this stage so we can show
# the contract surface in the repo. CI will eventually publish via
# `pact-broker publish` and we can flip to ignoring then.
# pacts/
pact-logs/

# Kubernetes
kubeconfig
*.kubeconfig

# Docker
docker-compose.override.yml

# Per-developer Claude Code permissions / bypass mode
.claude/settings.json
.claude/settings.local.json

# Vault raft snapshots (contain secrets)
vault-snapshots/

# IDE
*.code-workspace
.claude/worktrees/
