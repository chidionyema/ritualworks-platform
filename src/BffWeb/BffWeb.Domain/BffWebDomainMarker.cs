namespace Haworks.BffWeb.Domain;

/// <summary>
/// bff-web is a thin composition layer — no domain model of its own.
/// This marker exists only so the Domain assembly compiles and its
/// namespace is available to global usings in higher layers.
/// </summary>
public static class BffWebDomainMarker
{
}
