// L0 ships AuditWebAppFactory only. L1.B / L1.C / L1.D add their
// integration tests in this project (one test class per phase, files
// named per the parallel-tracks.md scope). Do not add tests here at the
// project root — keep them under feature subfolders.
namespace Haworks.Audit.Integration;

internal static class Placeholder
{
    // Intentionally empty. Delete once L1.B has landed EndToEndCaptureTests.
}
