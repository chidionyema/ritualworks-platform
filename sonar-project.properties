sonar.projectKey=chidionyema_haworks-platform
sonar.organization=chidionyema
sonar.projectName=haworks-platform

# Sources and tests
sonar.sources=src/
sonar.tests=tests/
sonar.exclusions=**/obj/**,**/bin/**,**/Migrations/**,**/*.Designer.cs

# Coverage report (generated by Coverlet in CI)
sonar.cs.opencover.reportsPaths=TestResults/**/coverage.cobertura.xml

# Encoding
sonar.sourceEncoding=UTF-8
